import React from "react";
import PropTypes from "prop-types";
import styles from "./ReviewFilterSelector.css";

export default class ReviewFilterSelector extends React.Component {
    constructor (props) {
        super (props)

        this.state = {};
    }

    render () {
        return <div className="review-filter-selector-container">This will be the Review Filter Selector</div>
    }
}